# Copy to android/key.properties and fill in (do not commit).
# storeFile can be absolute or relative to android/app/
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=upload
storeFile=upload-keystore.jks
