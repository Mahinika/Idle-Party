{
  "mcpServers": {
    "idle-party": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "${workspaceFolder}\\tool\\mcp_idle_party\\run.cmd"],
      "env": {
        "IDLE_PARTY_ROOT": "${workspaceFolder}"
      }
    }
  }
}
