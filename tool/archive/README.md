# Archived one-shot tools

Historical generators for docs under `docs/archive/` (feel audit / Play
polish 1000). Do not run as part of normal ship work.

Paths inside the scripts may still say `docs/…` — update locally if you
ever re-run them against `docs/archive/`.
