# Authored overlays for warrior

Hand/generated 128×128 PNGs win over extract in `build_owned_gear_layers.py`.

Shipped here: `helm_t0`/`helm_t2`, `cloak_t0`/`cloak_t2` (idle/walk/attack).
Shared weapons: `assets/custom/char/gear/_authored/sword_t0_*`, `shield_t0_*`.

Do **not** regenerate with invent scripts — `tool/author_warrior_gear.py` only lists files.
Rebuild live gear: `py tool/process_warrior_gen_art.py` then `py tool/build_owned_gear_layers.py`.
