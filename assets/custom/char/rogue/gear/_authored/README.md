# Authored overlays for rogue
Hand/generated 128×128 PNGs win over extract.

Shipped: `helm_t0` / `helm_t2` (idle/walk/attack).
Shared weapons: `assets/custom/char/gear/_authored/`.

Rebuild: `py tool/process_shared_weapon_art.py` then `py tool/build_owned_gear_layers.py`.
