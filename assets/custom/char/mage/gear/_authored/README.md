# Authored overlays for mage
Hand/generated 128×128 PNGs win over extract.

Shipped: `cloak_t0` / `cloak_t2` (idle/walk/attack).
Rebuild: `py tool/process_caster_cape_art.py` then `py tool/build_owned_gear_layers.py`.
